import App from './app/app';
import {AppRegistry} from 'react-native';
import 'react-native-gesture-handler';

// AppRegistry.registerComponent('tablevisit', () => App);
AppRegistry.registerComponent('TableVisitApp', () => App);

export default App;
